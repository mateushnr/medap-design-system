import { Meta } from '@storybook/addon-docs'
import { SpacesGrid } from '../../components/SpacesGrid'
import { formattedSpaces } from '../../components/SpacesGrid/formattedSpaces'

<Meta title="Tokens/Space" />

# Espaçamento

Tokens de espaçamento do design system do Medap

<SpacesGrid spaces={formattedSpaces} />
